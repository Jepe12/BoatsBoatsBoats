<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Home | Boats, Boats, Boats!</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link href="/assets/styles.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1.0,maximum-scale=1.4">
    <link rel="icon" type="image/x-icon" href="/favicon.png">
    <script src="/assets/helpers.js"></script>
    <script src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light ps-4">
        <a href="/"><img src="/favicon.png"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/products">Products</a>
                </li>
            </ul>
            <ul class="navbar-nav px-4">
                {{#if user}}
                <li class="my-2 my-lg-0">
                    <a class="nav-link" onclick="logout">Logout</a>
                </li>
                {{else}}
                <li class="my-2 my-lg-0">
                    <a class="nav-link" href="/login">Login</a>
                </li>
                {{/if}}
                {{#if cartSize}}
                <li class="my-2 ms-2 my-lg-0 cart pt-2">
                    <a class="text-decoration-none" style="font-size: 16px" href="/cart">{{ cartSize }}</a>
                </li>
                {{/if}}
                <li class="my-2 ms-2 my-lg-0 cart pt-1">
                    <a href="/cart"><iconify-icon icon="mdi:cart"></iconify-icon></a>
                </li>
                <li class="my-2 ms-2 my-lg-0 cart pt-1">
                    <a href="/order"><iconify-icon icon="mdi:format-list-bulleted"></iconify-icon></a>
                </li>
            </ul>
        </div>
    </nav>

    {{{body}}}

</body>
</html>